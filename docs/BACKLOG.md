> 除非必要否則不要讀取 docs/archive/ 內容,避免浪費 token。

# BACKLOG(已知但刻意未做)

| 項目 | 觸發條件 |
|---|---|
| Canvas/SVG 圖表取值(需讀圖表庫內部資料或 OCR) | 使用者提出實際目標頁是 canvas 圖表時 |
| 逐站 `optional_host_permissions` 取代 `<all_urls>` | 要上架 Chrome Web Store 時(審核會要求) |
| 自動落地 JSON(File System Access 選資料夾,需偶爾重授權) | 使用者需要不用手動就有檔案時 |
| Native Messaging host 寫任意路徑、完全無人值守 | 上一項仍不夠時(多一個安裝步驟) |
| 每個值各自的聚合方式 | 目前欄列聚合是全任務一份;使用者要對不同值用不同聚合時 |
| 每個值各自的位置定位 | AF-8 的「列定位／欄定位」是任務層級一份;使用者要對不同值用不同位置時 |
| 位置定位支援倒數第三筆以後 | 目前只有第一／最後／倒數第二;需要時把 `pos` 擴成 `{from, offset}` |
| 依列標題關鍵字自動排除合計列（合計／總計／Total） | AF-16 刻意不做：猜錯就靜默漏算，違反 SPEC §7「判準不放寬」。目前靠 tfoot 預設排除＋右鍵點選排除。使用者的表沒有 tfoot、合計列位置又會變，而且反映每次都要手點時 |
| 立即測試結果回頁面上色（採用／略過／排除三色標在表格上） | AF-17 只做面板內的明細表：回頁面要新訊息送進 frame、多值任務會混色、iframe 捲動與換頁後標示時機都要處理。使用者反映明細表對不回頁面上是哪幾格時 |
| 明細表點一列直接設成略過起訖或排除 | AF-17 刻意不做：會是第二個改 `skip` 的入口（頁面右鍵已能排除單列）。使用者反映數列數填略過很麻煩時 |
| ARIA 表格與 CSS 假表格的「空白格」判準另外驗證 | AF-17 的頭尾空白判準只看擷取端清單項的文字（`/^\s*$/`），三種表格來源共用；本輪只以 `<table>` 與帶 `inner` 的格子實測。有 ARIA／CSS 假表格站台回報「空白沒被略過」或「有內容卻被當空白」（例如只放圖示、`aria-label` 的格子）時 |
| 面板換目標重畫時 `#errors` 與 `#test-note` 不清 | 既有行為：`render` 清預覽、狀態色、診斷與明細表，錯誤文字與說明句留著（AF-17 體檢觀察）。有人反映改選目標後還看到上一個目標的錯誤訊息時，比照 `resetTestDetail` 一起清 |
| 立即測試明細表對編輯既有任務可用 | 跟著「立即測試對編輯既有任務開放」一起做（明細表只是它的結果呈現） |
| 每個值各自的略過頭尾（`skip`） | AF-16 的 `skip` 是任務層級一份（與聚合方式、位置定位同一層）；與「每個值各自的聚合方式／位置定位」兩條一起做 |
| ARIA 表格與 CSS 假表格的表尾自動排除 | AF-16 只認 HTML `<tfoot>`；ARIA 的 `rowgroup` 沒有標準的表尾語意。有這種站台回報合計被加進去時 |
| 排除項找不到時自動改用位置 | AF-16 排除項標題找不到只降黃燈、不排除；合計列標題每天會變（例如帶日期）的站台回報時，考慮「標題找不到但原本是最後一列 → 仍排最後一列」 |
| 選取模式排除標示的整欄／整列兩段迴圈、「整欄用列／整列用格」單位選字收成一份 | AF-16 終檢指出：`picker-mode.js` 的 `applyPickedMarks` 兩段逐字相近；單位選字在 `describe.js` 一處、`picker.js` 兩處、`extract.js` 兩處各寫一份。下次動排除標示或白話句時一併收（描述句仍以 `describe.js` 為唯一來源） |
| 「立即測試」以新分頁執行 | 目前沿用使用者眼前那個分頁(iframe 已開著),與排程行為不同,只用一行說明提醒;使用者反映測試會過但排程失敗時。AF-14 之後可先請他匯出診斷包看 `tabUrl` 與 `frame.candidates` |
| 排程失敗保留最新一份診斷包(`lastDebug:<taskId>`) | AF-14 只做「立即測試」;排程紀錄的 `error` 已經帶得出現況標題。使用者回報「試抓過得了、排程過不了」而訊息不夠用時再做(每個任務只留最新一份,避免 storage 長大) |
| 定位下拉挪到摘要卡提示句旁邊 | 那兩個下拉在「抓什麼」區(不在進階區),AF-14 用摘要卡提示句＋捷徑鈕 `#goto-rowpos` 送焦點過去;使用者反映還是找不到時再動版面 |
| 純數值標題「唯一出現才定位」的誤判 | 這是**刻意接受的取捨**(SPEC §7):存的 `4318` 碰巧在別列唯一出現會跟過去並亮 `fallback` 黃燈。使用者回報單列數值表天天黃燈、或抓到別列時,再加「唯一且位置相近」的限制 |
| 單值任務事後升級成多值 | 目前要多值就得重選建新任務(序列 id 一旦寫進紀錄就不能改);使用者反映舊任務要加值時 |
| 紀錄改用 `field` 欄位的複合鍵(甲案) | 序列 id 的字串接法造成問題時;目前單值任務零遷移是它最大的好處 |
| 圖表 X 軸可選 | 目前時間永遠是 X 軸;使用者要以別的維度比較時 |
| 排程觸發失敗立即寫紀錄 | 目前只有手動抓取這樣做(排程仍走重試策略);使用者反映看不到排程失敗時 |
| 折線超過 8 條的上限收成一份 | 目前 `drop-rules.MAX_CHART_SERIES` 與 `dashboard`、`trend-popover` 的硬編碼 `8` 三處並存;下次動到圖表上限時一併收成一份 |
| 卡片 TSV 不再掛在 `dataset.tsv` | 目前為了讓測試讀得到而序列化進 DOM 屬性;找到別的驗證方式時 |
| 面板寬度記憶／自訂 | side panel 的寬度由瀏覽器管理(實測預設 360px,使用者可拖寬);有人反映每次都要重拖時再看 |
| Chrome 114–141 沒有 `sidePanel.onClosed`：關掉面板後頁面標示會殘留到下一次選取 | 只是視覺殘留;有那個版本的使用者回報時再補輪詢或別的訊號 |
| 深色模式以外的主題色 | 使用者提出 |
| 位置定位的名稱後綴收成一份 | 現在 `picker.js` 的 `singleCellName`/`defaultPickName` 與 `main.js` 的 `defaultFieldName` 各一份（`POS_LABELS`/`POS_NAMES` 兩張同表）；下次動命名規則時搬進 `shared/` 讓兩端 import |
| 選取模式用 `Tab` 走到面板的「完成／取消」 | 目前 `Tab` 在工具列三段之間循環(既有行為,多處測試依賴),按鈕靠滑鼠點取得焦點;鍵盤使用者已有 `Enter` 送出與 `Esc` 取消。有人反映想用 `Tab` 走到按鈕時再改 |
| Report 頁與 popup 沿用 `ui/ui.css` | AF-7 只讓 picker 與 site 走它(Report 樣式量大、風險高);下次動 Report 樣式時順手搬 |
| 「立即測試」對編輯既有任務開放 | AF-7 改走 background 後技術上已可行(它會自己開分頁定位);使用者反映編輯時也想先試抓時 |
| 秒級抓取 | 目前最小 1 分鐘;alarms 下限 30 秒,再快要常駐分頁 |
| Firefox 支援 | 有需求時;選擇器與 alarms API 皆可移植,contextMenus 差異最大 |
| Chrome Web Store 與 Edge Add-ons 上架 | 使用者想分享給別人安裝時(兩邊分別審核) |
| 任務設定 `storage.sync` 自動同步 | 使用者常在多台機器切換且嫌手動匯入麻煩時 |
| 抓取時同時截圖存證 | 使用者需要查證抓到的值時 |
| Picker 設定 `attr` / `child` / `label` 策略的參數 | AF-5 已從下拉移除(選了必定失敗);`extract.js` 仍支援,既有任務重存也會保留原值。有站台需要時補上參數欄位再放回下拉 |
| 抓取期間週期性續命(每 20 秒呼叫 `getPlatformInfo`) | 目前只在開始時呼叫一次;有任務因 service worker 被回收而抓到一半失敗時 |
| Picker 設定乘數與小數位數 | `extract.js` 支援 `multiplier`/`decimals`,但只能靠匯入設定檔設定 |
| 任務層級的 `precheckLeadMinutes` 介面 | 程式支援,Picker 沒有欄位 |
| `lateTolerance`(遲到容忍度) | 規格寫了但沒實作;`late` 目前只來自補抓 |
| 同時最多 2 個站台並行的全域佇列 | 目前只有同站台串行;使用者任務多到互相拖慢時 |
| HTTP 錯誤頁(標題含 `ERR_`)視同 not_found | 使用者反映抓到錯誤頁卻被當成成功時 |
| 錯過清單項目超過 7 天自動略過 | 目前只限制新算入的範圍,既有項目不會過期 |
| 燈號的圖示變體(`setIcon`) | 圖示檔已產好(`icons/icon-{green,yellow,red,gray}-*.png`),但 `applyBadge` 目前只設 badge 不換圖 |
| 預檢 / 站台檢查 / 找不到元素的通知點擊路由 | 目前只有告警通知點得動 |
| popup 顯示「下次預檢」時間 | 目前只顯示下次抓取時間與暫停狀態 |
| 看門狗清掉卡住的執行後重新排入 | 規格 §4.1 寫「標 interrupted 並重新排入」,目前只清掉並記診斷;那個槽要等下一次排程 |
| Shadow DOM 內的元素 | 選取模式選不到目標(常見於 Web Components 站台) |
| 跨頁分頁的表格 | 要聚合的資料在第 2 頁以後時 |
| OAuth / 跨 origin 的登入流程 | 登入頁在別的網域時(目前只支援同 origin 的表單登入) |
| 同一站台多組帳號 | 使用者同一個網站有兩個帳號要分別抓時 |
| 「測試登入」按鈕(站台設定視窗內即時驗證) | 使用者反映設定完不知道對不對時 |
| 含密碼的設定匯出(換機沿用) | 使用者要換電腦時;本機金鑰的密文換機解不開,需改以密語重新加密 |
| `web_accessible_resources` 收斂成單一 bundle | 上架審核要求、或有人反映網頁可探測到本擴充功能時(需要打包步驟,與「無框架原生 JS」慣例衝突) |
| 煙霧測試裡「整頁換頁」的排程案會 60 秒不回應 | AF-13 的隔離探針證明這個任務本身是好的（單獨跑、連跑兩次、接在 iframe 任務之後都成功，取到換頁後的值 8888），但放進整輪煙霧就必定不回應；把它移出煙霧、原因未明。已排除：AF-13 的改動（dev 上只套 `autoDiscardable` 修正也重現過類似卡住）、段落順序（移到 AF-11 之前一樣）、殘留瀏覽器行程。下次動 `fetcher` 或煙霧時再追 |
| `background` 其餘沒有逾時的 `tabs.sendMessage` | AF-13 只把前置動作、捲動、擷取三則包進 `sendToFrame`(有逾時、會清計時器)。同型普查還有:`login.js` 三處(`CHECK_LOGIN`×2、`FILL_LOGIN`)、`frames.js:107`(`locateFrame` 收尾判定的 `RESOLVE_LOCATOR`,有 try/catch 但沒逾時)、`main.js` 七處(UI 面板相關)。回應遺失就吊到 service worker 被回收。有站台回報「抓取卡住不動」時,先從 `login.js` 與 `frames.js` 那四處加起 |
| `fetcher` 其餘錯誤訊息的中文化 | AF-13 只把「文件被換掉」那一類轉成中文,其餘仍是原文;使用者回報看不懂某則錯誤時 |
| 最上層目標的定位不做任何驗證 | `task.frame` 缺省時 `locateFrame` 直接回 `frameId 0`,不比對網址也不驗 locator;AF-13 用前置動作後的安定等待縮小風險,但沒有關掉。有使用者回報「抓到別的頁面的值」時,改成先驗 locator 解析得到 |
| 前置動作支援輸入文字、捲動到底 | 目前有 hover／等元素／點元素／等待四種;有站台需要時 |
| 前置動作 `hover` 之後的 `mouseout` 收尾 | 目前刻意不派(下一步通常要點那個選單);有站台需要「移開才收合」時 |
| 告警觸發時 POST 到 webhook | 使用者要接自動化時(`shared/alerts.js` 的 hits 就是觸發點) |
| 登入表單在 iframe 內 | 自動登入固定走最上層 frame(SPEC §6);有站台把登入表單放在 iframe 時 |
| `about:blank` / `srcdoc` iframe 的煙霧驗證 | 網址沒有辨識度,只剩 SPEC §3 第 3 層定位;有使用者回報這種站台時 |
| iframe 代理層貼進 iframe 自己的堆疊脈絡祖先 | 目前貼在 `<body>` 底下、`z-index` 取 iframe 最外層堆疊祖先的值(SPEC §2);選單與 iframe **同在一個有 z-index 的容器裡**時,代理層在 body 層級贏過整個容器,只剩讓路(零延遲收合的選單救不回來)。正解是把代理層貼進那個容器、`z-index` 取 iframe 自己的;代價是代理層會落在頁面的表格裡,`upgradeTarget`、`onMouseMove` 的表格分支、`onClick` 的表格分支三處都要加「代理層不算格子」的守衛。有使用者回報這種版面時 |
| 進入選取模式**之後**才插入的 iframe 沒有代理層 | 代理層在 `ENTER_PICK` 當下依當時的 `<iframe>` 清單建好(位置會隨滑鼠移動重算,但不會補建新的);有站台回報「後來出現的 iframe 指不到」時,改用 `MutationObserver` |
| 格內子路徑的鍵盤伸縮(在 `span` 與 `td` 之間切換子單位) | AF-15 子單位只有兩種規則(格內含表格取最內層格子、否則取最近帶文字的元素);使用者要選的層級剛好不是這兩種時 |
| 小表有多列時取每一列(路徑段落萬用) | AF-15 的 `inner` 固定到小表「第 r 列」,標籤明說「小表第 1 列第 2 格」;監控頁一台主機有多個告警要全抓時 |
| 小表自己的表頭定位 | `inner` 是結構路徑,小表內插欄就指到別格;使用者的小表有表頭且會插欄時 |
| `gridIndexOf` 處理上一列 `rowspan` 佔位 | AF-15 只處理同一列的 `colspan`(需要整張表的網格才算得出 `rowspan`);有 `rowspan` 資料列的站台回報抓錯格時 |
| ARIA 表格展開 `aria-colspan` | 擷取端與選取端目前都不展開(兩端一致);有 ARIA 表格用 `aria-colspan` 的站台回報時,兩端一起改 |
| `Ctrl+A` 全選子單位 | AF-15 的 `Ctrl+A` 一律全選整格;使用者要一次全選每一列的小表同一格時(目前可用右鍵「每格各一個值」) |
| 區塊擷取在清單（`<ul>`）來源沒有列元素 | `blockRowsOf` 只對 CSS 假表格補列元素,`source === 'list'` 的整列／單格帶 `inner` 一律取不到格子（訊息會說「取不到格子」）;有站台把要抓的值放在 `<ul>` 裡且需要子路徑時 |
| CSS 假表格的列若格內包 `<table>`,`rowCellsOf` 會把整列的格子濾光 | 既有缺陷（own filter 全濾光時回空）,AF-15 終檢實測;這種版型目前選取端與擷取端都取不到格子。有站台回報時改 `rowCellsOf` 對非 `tr` 的列退回 `children` |
| 右鍵選單「改抓外層表每一列的這個位置」 | AF-15 只靠面板提示句加 `↑`(八項選單太擠);使用者反映找不到 `↑` 這條路時 |
| 選取模式從 iframe 往上回到父頁面 | 目前只能往下鑽,要換目標得 `Esc` 重來;使用者反映麻煩時 |
| 英文介面 / i18n | 上架 Chrome Web Store 或要分享給非中文使用者時 |
| 從 Distill / Visualping 匯入任務 | 有使用者提出時 |
| 互動版獨立 HTML 報表(目前是靜態快照) | 使用者需要在匯出檔內切換範圍或篩選時 |
| 值未變化時不寫紀錄的選項 | 紀錄量成為問題、保留天數設定已不夠用時 |
| 既有三處拖曳(卡片移動縮放、儀表板頁籤、任務清單)改用 `ui/report/dnd.js` | 那三處要改動時順手搬;AF-4 只讓新拖曳走它,重構既有的風險大於收益 |
| 樞紐表桶內多筆的其他合併策略(平均、最大、加總) | 目前固定取最新的成功值;使用者提出要別的統計時 |
| interval 任務的錯過補抓 | 目前 interval 不進錯過清單(一個週末上百槽);使用者明確要求補抓時 |
| `pruneCardsForTask` 刪掉整張卡片不可復原 | 任務刪除時來源歸零的卡片會被整張移除,undo 只在編輯模式的記憶體內;使用者反映誤刪時 |
| 歷史頁的「依一天中的時刻分組」略過沒有 slot 的紀錄(`logic.js`) | 使用者反映補抓/手動紀錄在該視圖缺席時 |
| 非編輯模式仍渲染隱藏的移除把手(匯出 HTML 夾帶死元素) | 匯出檔大小成為問題時;需改成切換編輯模式時重繪卡片 |
| 任務刪除/停用後已排定的重試 alarm 不會被看門狗清掉(觸發時會被擋,純殘留) | 有人反映 alarm 清單有垃圾時 |
| 拖曳投放的行動裝置/觸控適配 | 目前只在桌面滑鼠驗證過;有平板需求時 |
| 拖曳的鍵盤替代操作(無障礙) | 有鍵盤或輔助技術使用者提出,或上架審核要求時 |
| 儀表板卡片虛擬化 | 單一儀表板卡片超過 30 張且重畫變慢時 |
| 儀表板匯出成圖片(PNG) | 使用者要把儀表板貼進簡報時 |
| 從設定面板回頁面加選一個值 | AF-10 已做「換目標不重置表單」;要「加選」而不是「換」還需要把既有 picks 帶回選取模式,使用者反映時做 |
| 編輯既有任務時也能回頁面重選目標(等於把 repick 搬進面板) | AF-10 的面板讓它變容易;使用者反映任務頁的重選要另開分頁很麻煩時 |
| 編輯任務時把面板開在目標網址的分頁上 | 目前開在 Report 分頁旁;需要找分頁與「分頁不存在」的分岔,使用者反映編輯時想同時看到頁面時 |
| 選取模式面板可拖曳 | AF-9 已做「游標靠近就換角」的自動閃避;閃避還是擋到時再做 |
| 多值清單的上／下移按鈕停用時也給不出理由(原生 `disabled`,點了完全沒回饋) | 下次動多值清單時,比照工具列的做法補一句原因 |
| Picker 的星期／卡片型別 chip 改成真正的 `button[aria-pressed]` | 目前是 checkbox 加 chip 外觀(保留原生勾選語意);有無障礙需求或要支援鍵盤切換時再改 |
| 多任務一次建立：逐任務的進階設定（定位方式、略過頭尾、正規表達式、前置動作、告警） | AF-18 第一版刻意做薄（各自命名＋共用排程與去處），要做得把單任務表單做成可重複的元件。使用者反映存完再一個個編輯很麻煩時 |
| 多任務一次建立的其他入口（popup）與跨框架同一批 | AF-18 使用者指定入口只在右鍵；一個選取階段只活在一個框架。有人反映找不到入口、或要同時抓外層與框架內的值時 |
| 多任務整批合併成一張儀表板卡片 | 目前每個任務各自建卡片（與逐一建立結果相同），受折線 8 條上限牽制。使用者反映儀表板被灌滿時 |
| 同一頁同一槽的失敗通知合併 | 批次建立讓「同一頁 N 個任務」變常見，頁面掛掉時一次跳 N 則。使用者反映被通知洗版時 |
| 「整欄→每格」自動納入之後新增的列 | AF-18 的每格值是選取當下那幾格（教學頁有寫）。使用者反映新主機上線沒被抓到時 |
| 工具列「整列→每格」 | AF-18 工具列四段已是窄頁上限，整列每格留在右鍵選單。有人反映找不到時 |
| 單欄小表上的「整列」自動升到外層 | AF-18 只做整欄（監控頁實例）。有站台是「每一欄的某一格各包一張單欄小表」時 |
| `repick` 的升到外層 | 升上去值的 key 會重生、歷史序列斷掉，需要新舊值對應方案。使用者要把既有任務改成抓外層整欄時 |
| ARIA／CSS 假表格巢狀的升到外層 | AF-18 只對 HTML `<table>` 啟用（沒有實測樣本）。有這種站台回報時 |
| 完整的多步復原堆疊 | 目前是單步快照（AF-18 讓移除類動作也存快照）。使用者反映連續誤點救不回來時 |
| 教學頁的其他入口（popup、首次安裝自動開、面板上的「？」） | AF-18 使用者指定只放右鍵（設定頁開關旁留連結）。有人反映找不到教學時 |
| 教學頁可動手的練習表格 | 擴充功能頁不能注入 content script，要另做示範模式。教學頁的示意圖仍看不懂時 |
| 選取模式批次送出時 `confirmPick` 的框架守門沒有測試 | AF-18 G-1 實作回報：點擊那道有測試，`Enter`／完成鈕停在代理層上且只剩元素組時才走得到的那道沒有。動 `confirmPick` 時補 |
